# залежності — Render ставить їх сам через npm install
node_modules/

# рантайм-стан бота (створюється на сервері, в репозиторії йому не місце)
marble_state.json

# локальні конфіги розробки
.claude/
